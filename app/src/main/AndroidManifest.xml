<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.technopark.smartbiz"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".userIdentification.LoginActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="com.vk.sdk.VKServiceActivity"
            android:label="ServiceActivity"
            android:theme="@style/VK.Transparent"
            />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"
            >
        </activity>
        <activity
            android:name=".buisnessLogic.addProduct.AddProductActivity"
            android:label="@string/title_activity_add_product"
            android:theme="@style/AppTheme.NoActionBar"
            >
        </activity>
        <activity
            android:name=".screnListView.ListAddedProducts"
            android:label="@string/title_activity_list_added_products"
            android:theme="@style/AppTheme.NoActionBar"
            >
        </activity>

        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

        <provider
            android:name=".database.SmartShopContentProvider"
            android:authorities="ru.tech_mail.smart_biz.data"
            />

        <activity
            android:name=".PurchaseActivity"
            android:label="@string/title_activity_purchase"
            >
        </activity>
    </application>

</manifest>
